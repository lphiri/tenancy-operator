---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tenantprofiles.tenancy.opendatahub.io
spec:
  group: tenancy.opendatahub.io
  names:
    kind: TenantProfile
    listKind: TenantProfileList
    plural: tenantprofiles
    shortNames:
    - tprof
    singular: tenantprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .spec.defaults.maxProjects
      name: MaxProjects
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TenantProfile is the Schema for the tenantprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TenantProfile
            properties:
              admins:
                description: |-
                  admins are subjects with administrative authority over this tenant.
                  Ancestor admins additionally inherit authority via the hierarchy.
                items:
                  description: Subject identifies a user, group, or service account
                    that holds authority.
                  properties:
                    kind:
                      description: kind is the RBAC subject kind.
                      enum:
                      - User
                      - Group
                      - ServiceAccount
                      type: string
                    name:
                      description: name is the subject name.
                      minLength: 1
                      type: string
                    namespace:
                      description: namespace is required only when kind is ServiceAccount.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              defaults:
                description: defaults are applied to TenantProjects created in this
                  tenant.
                properties:
                  maxProjects:
                    default: 0
                    description: |-
                      maxProjects caps how many TenantProjects this tenant may own. Zero (the
                      restrictive default) means no projects until an admin raises it.
                    format: int32
                    minimum: 0
                    type: integer
                  networkIsolation:
                    default: tenant
                    description: networkIsolation is the default isolation preset
                      for new projects.
                    enum:
                    - none
                    - tenant
                    - strict
                    type: string
                type: object
              tenant:
                description: |-
                  tenant is the name of the PlatformTenant this profile configures. It is
                  set at creation and immutable (enforced by webhook).
                minLength: 1
                type: string
            required:
            - tenant
            type: object
          status:
            description: status defines the observed state of TenantProfile
            properties:
              conditions:
                items:
                  properties:
                    lastHeartbeatTime:
                      description: |-
                        The last time we got an update on a given condition, this should not be set and is
                        present only for backward compatibility reasons
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human-readable message indicating
                        details about the transition.
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        The value should be a CamelCase string.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        When this is not specified, it defaults to Error.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              observedGeneration:
                description: The generation observed by the resource controller.
                format: int64
                type: integer
              phase:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
