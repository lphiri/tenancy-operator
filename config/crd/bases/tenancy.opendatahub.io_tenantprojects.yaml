---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tenantprojects.tenancy.opendatahub.io
spec:
  group: tenancy.opendatahub.io
  names:
    kind: TenantProject
    listKind: TenantProjectList
    plural: tenantprojects
    shortNames:
    - tproj
    singular: tenantproject
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TenantProject is the Schema for the tenantprojects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TenantProject
            properties:
              displayName:
                description: displayName is a human-friendly name for the project.
                type: string
              networkIsolation:
                description: |-
                  networkIsolation is the isolation preset for the project namespace.
                  Empty means inherit the tenant default.
                enum:
                - none
                - tenant
                - strict
                type: string
              tenant:
                description: tenant is the name of the owning PlatformTenant. Immutable
                  (webhook).
                minLength: 1
                type: string
              users:
                description: users are granted access to the project namespace via
                  RoleBindings.
                items:
                  description: ProjectUser grants a subject a role within the project
                    namespace.
                  properties:
                    kind:
                      description: kind is the RBAC subject kind.
                      enum:
                      - User
                      - Group
                      - ServiceAccount
                      type: string
                    name:
                      description: name is the subject name.
                      minLength: 1
                      type: string
                    namespace:
                      description: namespace is required only when kind is ServiceAccount.
                      type: string
                    role:
                      description: role is the access level granted, mapped to the
                        built-in edit/view roles.
                      enum:
                      - edit
                      - view
                      type: string
                  required:
                  - kind
                  - name
                  - role
                  type: object
                type: array
            required:
            - tenant
            type: object
          status:
            description: status defines the observed state of TenantProject
            properties:
              conditions:
                items:
                  properties:
                    lastHeartbeatTime:
                      description: |-
                        The last time we got an update on a given condition, this should not be set and is
                        present only for backward compatibility reasons
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human-readable message indicating
                        details about the transition.
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        The value should be a CamelCase string.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        When this is not specified, it defaults to Error.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              namespace:
                description: namespace is the name of the provisioned project Namespace.
                type: string
              observedGeneration:
                description: The generation observed by the resource controller.
                format: int64
                type: integer
              phase:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
